# Audio assets

Place these tracks here so the page can load them:

| File | Use |
|------|-----|
| `king-of-the-weeds.mp3` | Featured single — King of the Weeds |
| `echostory-arc.mp3` | EchoStory arc playlist (4-track saga) |

Links in `hydrilla_gorilla_echoverse_page.html` point to `audio/` (e.g. `audio/king-of-the-weeds.mp3`).

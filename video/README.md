# Video assets

Place video files here for the Video section on the page.

## Current

- **Demo video:** Add your main demo (e.g. `demo.mp4` or `king-of-the-weeds.mp4`). The page uses `video/demo.mp4` by default. Use a single primary file for the main player.

## Future (per episode)

- **Comic flip** — panel flip-through with captions
- **2 videos per episode** — e.g. main visualizer + 30s social trailer

The Video section is structured for: one main player now, plus placeholder slots for shorts and “comic flip + 2 per ep” when you add them.
